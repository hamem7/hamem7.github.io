# دليل تطبيق قواعد أمان Firebase — منصة دار حم

## اللي اتعمل

راجعت الكود الفعلي (core/firebase.js، database/*.js، student/login.html) وأكدت إن التطبيق
**مفيهوش أي تسجيل دخول حقيقي** — لا للمعلم ولا للطلاب. صفحة "تسجيل الدخول" بتاعة الطالب هي
مجرد اختيار اسم من قائمة، بلا كلمة سر.

معنى ده: أمان بياناتكم السحابية (Firestore + Storage) كان معتمد بالكامل على قواعد الأمان
المكتوبة في لوحة تحكم Firebase — ومفيش ملف قواعد كان موجود أصلاً في المشروع أراجعه، فمكنش في
طريقة أتأكد بيها إيه المفعّل فعليًا.

كتبت ملفين جاهزين للصق مباشرة في لوحة تحكم Firebase:

- **`firestore.rules`** — قواعد قاعدة بيانات Firestore (الواجبات والتسليمات)
- **`storage.rules`** — قواعد التخزين (التسجيلات الصوتية)

## خطوات التطبيق

1. افتح [Firebase Console](https://console.firebase.google.com) → مشروع `darham-quran`.
2. من القائمة الجانبية: **Firestore Database** → تبويب **Rules** أعلى الصفحة.
3. امسح المحتوى الموجود، والصق بدلاً منه محتوى ملف `firestore.rules` كاملاً.
4. اضغط **Publish**.
5. من القائمة الجانبية: **Storage** → تبويب **Rules**.
6. امسح المحتوى الموجود، والصق محتوى ملف `storage.rules` كاملاً.
7. اضغط **Publish**.

بعد النشر، جرّب سيناريو كامل: نشر واجب من شاشة المعلم → حل الطالب للواجب وتسليمه (مع سؤال
صوتي لو موجود) → فتح شاشة تصحيح المعلم والتأكد إن النتيجة والصوت ظاهرين. لو أي خطوة فشلت
برسالة "Missing or insufficient permissions"، ابعتلي رسالة الخطأ بالظبط وأظبط القاعدة المسؤولة.

## اللي اتقفل فعليًا بالقواعد دي

- محدش يقدر يمسح واجب أو تسليم من برا التطبيق (delete مرفوضة تمامًا).
- محدش يقدر "يسرد" كل الواجبات دفعة واحدة (list مرفوضة) — يقدر بس يجيب واجب بمعرفه المحدد.
- أي بيانات مرسلة (واجب جديد، تسليم جديد) لازم تطابق الشكل المتوقع تمامًا (نوع الحقول،
  الحجم، عدم وجود تاريخ مستقبلي)، فمرفوض أي عبث أو حقن بيانات عشوائية.
- تعديل تسليم موجود محصور فعليًا في حقلي الدرجة والتفاصيل فقط (score, details) — نفس
  الحقلين اللي بترسلهم شاشة التصحيح بالظبط — فمستحيل حد يغيّر اسم الطالب أو الواجب في
  تسليم قائم.
- رفع الصوت محدود بـ8 ميجابايت ولازم يكون ملف صوت فعلاً (مش أي نوع ملف).
- أي مسار أو مجموعة بيانات تانية غير متوقعة (مش homeworks ولا submissions) مرفوضة بالكامل
  افتراضيًا (deny by default) — لو حد حاول يستحدث مسار جديد في قاعدة بياناتكم، هيترفض تلقائيًا.

## اللي لسه مفتوح — وليه (القرار اللي محتاج تاخده)

سطرين في `firestore.rules` وسطر في `storage.rules` معلّمين بـ ⚠️ لأنهم **لازم** يفضلوا
مفتوحين (`if true`) حاليًا، مش لإهمال، لكن لأن شاشة المعلم نفسها بتعمل نفس الطلبات دي
بالظبط من غير أي هوية موثقة — فلو قفلناها هتمنع المعلم عن استخدام تطبيقه هو نفسه:

- **قراءة/سرد كل تسليمات الطلاب** (`submissions: allow get, list`) — مستخدمة في شاشة
  "الواجبات تحتاج تصحيح" والتقرير الشهري. حاليًا أي حد يعرف اسم المشروع (`darham-quran`،
  ظاهر في كود الصفحة لأي زائر) يقدر يقرأ أسماء ودرجات كل الطلاب.
- **قراءة التسجيلات الصوتية** (`storage: allow read`) — نفس المنطق: لازم تفضل مفتوحة
  عشان المعلم يقدر يسمع تسجيلات الطلاب، لكن ده معناه أي حد يعرف/يخمّن مسار الملف يقدر
  يسمعه هو كمان.

**الحل الحقيقي الوحيد لقفل النقطتين دول بالكامل**: تسجيل دخول حقيقي للمعلم (Firebase
Authentication ببريد وكلمة سر واحدة)، بحيث تستبدل `if true` بشرط زي
`request.auth.token.email == 'بريد المعلم'`. ده مش محتاج تغيير أي حاجة في تجربة الطلاب
(لسه بيدخلوا بدون كلمة سر زي ما هو دلوقتي)، بس شاشات المعلم (نشر الواجبات، التصحيح،
التقارير) هتحتاج تسجيل دخول مرة واحدة على جهازه.

**حل وسيط سريع ومفيدش يغيّر تجربة حد على الإطلاق**: تفعيل **Firebase App Check**
(reCAPTCHA v3 خفي بالكامل، محدش بيشوفه) — ده بيتأكد إن الطلب جاي فعلاً من تطبيقكم المنشور،
مش من سكربت أو أداة خارجية (زي curl) بتضرب الـ API مباشرة بمعرفة اسم المشروع بس. ده بيقفل
التهديد الأكبر (زائر عشوائي من الإنترنت) من غير ما يحل مشكلة "أي حد بيستخدم تطبيقكم الفعلي
بيقدر يشوف بيانات كل الطلاب" — دي محتاجة تسجيل دخول المعلم فعلاً.

**قرارك:** لو عايز، أقدر أضيف تسجيل دخول حقيقي للمعلم (شاشة بسيطة، بريد وكلمة سر واحدة
مخزنة في Firebase Authentication) في خطوة تالية، ونظبط القاعدتين المعلّمتين بـ ⚠️ فوق
عشان تتقفل بالكامل.
