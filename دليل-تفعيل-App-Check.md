# دليل تفعيل Firebase App Check — قبل نشر المستودع Public

## ⚠️⚠️ [تحديث 2026-09-24] الدليل ده بقى قديم جزئياً — خطوة 4 تحديداً بطلت متاحة

اكتشفنا (بعد مشكلة حقيقية منعت رفع كل الواجبات وتسليمات الطلاب للسحابة بشكل شبه دائم — لوحة
App Check كانت بتوضّح إن 94% من الطلبات "Unverified") إن **Firebase أوقف تمامًا تسجيل
reCAPTCHA v3 العادي كخيار جديد** في App Check — خانة إدخال site key بتاعته بقت معطّلة تمامًا
في لوحة التحكم، وبيظهر تحذير أحمر "reCAPTCHA has been deprecated. Please use reCAPTCHA
Enterprise instead". يعني خطوة 4 تحت (اختيار reCAPTCHA v3) **بطلت ممكنة التنفيذ من الأساس**.

**الحل اللي طبّقناه فعليًا:** التطبيق كان أصلاً مسجَّل (من غير قصد على ما يبدو) تحت
**reCAPTCHA Enterprise** في نفس لوحة App Check، وFirebase بينشئ site key خاص بـ Enterprise
تلقائيًا وقت هذا التسجيل. جبنا هذا المفتاح (Firebase Console → App Check → Apps → DarHamWeb →
reCAPTCHA Enterprise → يظهر فيه الـ site key)، وعدّلنا `core/firebase.js` يستخدم
`ReCaptchaEnterpriseProvider` بدل `ReCaptchaV3Provider` بهذا المفتاح الجديد (راجع التعليق
🌟🌟 المفصّل بجانب `RECAPTCHA_ENTERPRISE_SITE_KEY` في الملف نفسه). **خطوات 4 و5 تحت أصبحت
تاريخية فقط (توضّح كيف كان الإعداد الأصلي المقصود)، ومش المسار المتبع فعليًا حالياً.**

**ملحوظة تستحق المتابعة:** reCAPTCHA Enterprise له حصة مجانية شهرية سخية، لكنه عادةً يحتاج
تفعيل Billing (فوترة) على مشروع Google Cloud المرتبط بمشروع Firebase — لو ظهرت مشاكل رفع
جديدة رغم هذا الإصلاح، أول حاجة تتأكد منها هي حالة الفوترة دي في Google Cloud Console.

## ليه محتاجين ده دلوقتي بالذات

زي ما هو موضّح في `دليل-تطبيق-قواعد-الأمان.md`، قواعد Firestore وStorage الحالية مضطرة
تسيب قراءة تسليمات الطلاب (أسماء، درجات) والتسجيلات الصوتية مفتوحة (`if true`)، لأن شاشة
المعلم نفسها بتستخدمها بدون تسجيل دخول حقيقي لسه. طالما المستودع كان غير منشور، مفيش طريقة
سهلة لأي حد يعرف معرّف المشروع (`darham-quran`). لو المستودع بقى **Public**، أي زائر يقدر
يفتح `core/firebase.js` مباشرة ويشوف المعرّف، ويستخدمه لقراءة بيانات كل الطلاب من برا
التطبيق تمامًا.

**App Check** بيقفل التهديد ده تحديدًا: بيتأكد إن أي طلب واصل لـ Firestore/Storage جاي فعلاً
من نسخة المنصة المنشورة (عبر reCAPTCHA v3 يشتغل تلقائيًا في الخلفية، محدش بيشوفه أو يتفاعل
معاه)، مش من سكربت خارجي بيضرب الـ API مباشرة. ⚠️ **ده مش بديل عن تسجيل دخول المعلم** — لسه
أي حد يستخدم التطبيق الفعلي المنشور يقدر يشوف بيانات كل الطلاب. الفرق إن الزائر العشوائي من
الإنترنت (اللي مش بيفتح التطبيق أصلاً) بقى مقفول عليه. القرار الخاص بتسجيل دخول المعلم لسه
معلّق زي ما هو، لجلسة قادمة.

## اللي اتعمل بالفعل في الكود

أضفنا في `core/firebase.js` تهيئة App Check (بعد `initializeApp` مباشرة)، بنفس أسلوب
الاستيراد الكسول (Dynamic Import) ومعالجة الأخطاء المستخدم أصلاً مع Storage وAnalytics —
لو المفتاح مش متضاف، الكود يتجاهل نفسه تمامًا بأمان ولا يكسر أي شاشة. **دلوقتي الكود ده
خامل (inactive)** لحد ما تحط مفتاح reCAPTCHA v3 الحقيقي بدل `PASTE_YOUR_RECAPTCHA_SITE_KEY_HERE`.

## خطوات التفعيل (لازم تتعمل من لوحة تحكم Firebase — مش ممكن تلقائيًا)

1. افتح [Firebase Console](https://console.firebase.google.com) → مشروع `darham-quran`.
2. من القائمة الجانبية، تحت قسم **Build** (أو **Release & Monitor** حسب واجهة حسابك):
   افتح **App Check**.
3. هتلاقي تطبيق الويب بتاعك مُدرج — اضغط **Register** (تسجيل) بجانبه.
4. اختار **reCAPTCHA v3** كمزود الحماية. Firebase هيطلب منك site key من reCAPTCHA:
   - لو معندكش واحد، افتح [صفحة إنشاء reCAPTCHA](https://www.google.com/recaptcha/admin/create)،
     اختار **reCAPTCHA v3**، وضيف الدومينات اللي المنصة هتشتغل عليها (مثلاً
     `username.github.io`، وأي دومين مخصص لو أضفته لاحقًا، وممكن تضيف `localhost` مؤقتًا
     للتجربة المحلية).
   - انسخ **Site Key** الناتج، والصقه في خانة التسجيل في Firebase Console.
5. بعد التسجيل، افتح `core/firebase.js` في المشروع، ولاقي السطر:
   ```js
   const RECAPTCHA_V3_SITE_KEY = "PASTE_YOUR_RECAPTCHA_SITE_KEY_HERE";
   ```
   واستبدل القيمة بالـ Site Key اللي نسخته.
6. ارفع التعديل ده (push) للمستودع، وجرّب تفتح المنصة من الرابط المنشور فعليًا — افتح أدوات
   المطور (Console) وتأكد إنه مفيش أي خطأ متعلق بـ App Check.
7. ارجع لـ Firebase Console → **App Check** → تبويب **APIs**. هتلاقي **Cloud Firestore**
   و **Cloud Storage** مدرجين. لكل واحد فيهم اضغط الثلاث نقاط (⋮) واختار **Enforce**.
   ⚠️ **مهم جدًا**: الكود وحده مش كافي — لازم تضغط Enforce هنا فعليًا، وإلا الحماية دي بتفضل
   غير مُفعّلة رغم وجود الكود.

## للتجربة المحلية قبل النشر (localhost أو فتح الملف مباشرة)

reCAPTCHA v3 بيحتاج دومين مسجّل، فـ `localhost` أو فتح `index.html` مباشرة من جهازك ممكن
يفشل في التحقق حتى لو المفتاح صحيح. الحل المؤقت أثناء التطوير:

1. افتح Console المتصفح وأنت شغّال المنصة محليًا، واكتب:
   ```js
   self.FIREBASE_APPCHECK_DEBUG_TOKEN = true;
   ```
   قبل ما يحمّل `core/firebase.js` (أسهل طريقة: أضفها مؤقتًا كسطر `<script>` في `index.html`
   قبل سطر تحميل `core/app.js`، وامسحها بعد ما تخلص تجربة).
2. أعد تحميل الصفحة — هيطبع في الـ Console رسالة فيها **Debug Token** (نص عشوائي طويل).
3. انسخه، وفي Firebase Console → App Check → التطبيق → **Manage debug tokens**، ضيفه هناك.
4. دلوقتي التجربة المحلية هتشتغل حتى بعد تفعيل Enforce، لحد ما تمسح خطوة 1 من الكود.

## بعد التفعيل

المستودع بقى آمن أكتر للنشر Public من ناحية زوار الإنترنت العشوائيين. القرار الخاص بإضافة
تسجيل دخول حقيقي للمعلم (لقفل الثغرة بالكامل حتى من مستخدمي التطبيق نفسه) لسه معلّق، ونقدر
نرجعله في جلسة قادمة براحتك.
